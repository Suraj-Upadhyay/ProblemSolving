// You can specify default parameters to a function the following way.

// Only change code below this line

const increment = (number, value = 1) => number + value;

// Only change code above this line
