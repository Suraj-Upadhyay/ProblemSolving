/* Argument : The values which are inputted when the function is called.
   Parameter : The variables in the function which store the inputs passed to it.
*/

function functionWithArgs(a, b) {
    console.log(a + b)
}
  
functionWithArgs(1, 2);
  
