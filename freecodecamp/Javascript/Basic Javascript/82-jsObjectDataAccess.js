// If the name of the property you want to access doesn't contain spaces,
// you need to use the bracket ('[ ]') operator with quotes around properties' name.

// Setup
var testObj = {
    "an entree": "hamburger",
    "my side": "veggies",
    "the drink": "water"
};

// Only change code below this line

var entreeValue = testObj["an entree"];   // Change this line
var drinkValue = testObj["the drink"];    // Change this line  
