// typeof var-name;
// The above statement gives the type of the variable var-name.

// Setup
function compareEquality(a, b) {
    if (a === b) { // Change this line
      return "Equal";
    }
    return "Not Equal";
}

compareEquality(10, "10");
